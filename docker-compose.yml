version: '3.3'
services:
  bot:
    build: .
    command: sh -c "python -m bot"
    restart: always
    ports:
      - "5000:5000"
    volumes:
      - ./db/:/root/databases/

  sqlite3:
    image: sqlite3:latest
    stdin_open: true
    tty: true
    volumes:
      - ./db/:/root/databases/